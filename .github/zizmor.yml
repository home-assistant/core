rules:
  anonymous-definition:
    ignore:
      - check-requirements.lock.yml
  ref-version-mismatch:
    ignore:
      - check-requirements.lock.yml
  template-injection:
    ignore:
      - check-requirements.lock.yml
  undocumented-permissions:
    ignore:
      - check-requirements.lock.yml
  unsound-condition:
    ignore:
      # gh-aw emits an unsound folded fork guard; remove after it is fixed upstream.
      - check-requirements.lock.yml:74:5
