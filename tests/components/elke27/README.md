# Elke27 tests

This folder contains unit tests for the `elke27` integration.

Live hardware tests belong in the upstream `elke27` client library test suite so
Home Assistant Core CI does not carry opt-in panel-specific tests.
