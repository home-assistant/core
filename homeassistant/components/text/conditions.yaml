is_equal_to:
  target:
    entity:
      - domain: text
      - domain: input_text
  fields:
    behavior:
      required: true
      default: any
      selector:
        select:
          translation_key: condition_behavior
          options:
            - all
            - any
    value:
      required: true
      selector:
        text:
