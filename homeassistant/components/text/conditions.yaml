is_equal_to:
  target:
    entity:
      - domain: text
      - domain: input_text
  fields:
    behavior:
      required: true
      default: any
      selector:
        automation_behavior:
          mode: condition
    for:
      required: true
      default: 00:00:00
      selector:
        duration:
    value:
      required: true
      selector:
        text:
