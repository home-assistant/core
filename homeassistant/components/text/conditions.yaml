is_equal_to:
  target:
    entity:
      - domain: text
      - domain: input_text
  fields:
    behavior:
      required: true
      default: any
      selector:
        automation_behavior:
          translation_key: condition_behavior
          mode: condition
    value:
      required: true
      selector:
        text:
