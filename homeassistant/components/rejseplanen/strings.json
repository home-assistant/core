{
  "config": {
    "step": {
      "user": {
        "description": "Set up Rejseplanen data service."
      }
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    }
  },
  "config_subentries": {
    "stop": {
      "entry_type": "Stop",
      "step": {
        "user": {
          "description": "Set up stop subentry for Rejseplanen",
          "title": "Add Stop"
        }
      },
      "initiate_flow": {
        "user": "Add stop"
      }
    }
  }
}
