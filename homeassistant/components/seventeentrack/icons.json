{
  "entity": {
    "sensor": {
      "not_found": {
        "default": "mdi:package-variant-closed"
      },
      "in_transit": {
        "default": "mdi:package-variant-closed"
      },
      "expired": {
        "default": "mdi:package-variant-closed-remove"
      },
      "ready_to_be_picked_up": {
        "default": "mdi:package-variant-closed"
      },
      "undelivered": {
        "default": "mdi:package-variant-closed"
      },
      "delivered": {
        "default": "mdi:package-variant-closed-check"
      },
      "returned": {
        "default": "mdi:package-variant-closed-remove"
      },
      "package": {
        "default": "mdi:package-variant-closed"
      }
    }
  },
  "services": {
    "get_packages": "mdi:package-variant-closed",
    "add_package": "mdi:package-variant-closed-plus"
  }
}
