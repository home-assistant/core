type: vertical-stack
cards:
  # Motivation Message
  - type: markdown
    content: |
      💡 **Daily Motivation**
      
      {{ states('sensor.habitica_daily_motivation') }}
  
  # Habit Statistics
  - type: horizontal-stack
    cards:
      - type: entity
        entity: sensor.habits_daily
        name: Daily Habits
        icon: mdi:calendar-today
      - type: entity
        entity: sensor.habits_weekly
        name: Weekly Habits
        icon: mdi:calendar-week
      - type: entity
        entity: sensor.habits_monthly
        name: Monthly Habits
        icon: mdi:calendar-month
  
  # Individual Habit Example (with buttons)
  - type: horizontal-stack
    cards:
      - type: button
        entity: button.demo_habit_down
        name: −
        icon: mdi:minus-circle
        tap_action:
          action: toggle
      - type: entity
        entity: sensor.demo_habit
        name: Demo Habit
        secondary_info: last-changed
      - type: button
        entity: button.demo_habit_up
        name: +
        icon: mdi:plus-circle
        tap_action:
          action: toggle
  
  # Habit Details
  - type: markdown
    content: |
      **{{ state_attr('sensor.demo_habit', 'text') }}**
      
      📝 {{ state_attr('sensor.demo_habit', 'notes') }}
      
      ✅ Completed: {{ state_attr('sensor.demo_habit', 'counter_up') }} times
      ❌ Missed: {{ state_attr('sensor.demo_habit', 'counter_down') }} times
      🔄 Resets: {{ state_attr('sensor.demo_habit', 'frequency') }}
      
      Current Score: **{{ states('sensor.demo_habit') }}**
