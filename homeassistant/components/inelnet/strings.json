{
  "config": {
    "abort": {
      "already_configured": "This controller is already configured."
    },
    "error": {
      "cannot_connect": "Cannot connect to the controller. Check IP and network.",
      "invalid_channels": "Invalid channels. Enter positive channel numbers comma-separated.",
      "invalid_host": "Invalid IP address or hostname."
    },
    "step": {
      "user": {
        "data": {
          "channels": "Channels",
          "host": "IP address or hostname"
        },
        "data_description": {
          "channels": "Channel numbers comma-separated (positive integers).",
          "host": "e.g. 192.168.1.67 or my-controller.local"
        },
        "description": "Define all channels in this step. Enter the controller IP address or hostname and all channel numbers as a comma-separated list (e.g. 1 or 1,2,3). One cover device will be created per channel.",
        "title": "Configure INELNET"
      }
    }
  },
  "device": {
    "channel": {
      "name": "INELNET Blinds channel {channel}"
    }
  },
  "options": {
    "error": {
      "invalid_channels": "Invalid channels. Enter positive channel numbers comma-separated (e.g. 1,2,3)."
    },
    "step": {
      "init": {
        "data": {
          "channels": "Channels"
        },
        "data_description": {
          "channels": "Channel numbers comma-separated (e.g. 1,2,3)."
        },
        "description": "Add or remove channels for this controller. One cover device is created per channel.",
        "title": "INELNET channels"
      }
    }
  }
}
