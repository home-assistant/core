.trigger_common: &trigger_common
  target:
    entity:
      domain: schedule
  fields:
    behavior:
      required: true
      default: any
      selector:
        automation_behavior:
          mode: trigger
    for:
      required: true
      default: 00:00:00
      selector:
        duration:

turned_off: *trigger_common
turned_on: *trigger_common
