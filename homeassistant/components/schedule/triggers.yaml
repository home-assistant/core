.trigger_common: &trigger_common
  target:
    entity:
      domain: schedule
  fields:
    behavior:
      required: true
      default: each
      selector:
        automation_behavior:
          mode: trigger
    for:
      required: true
      default: 00:00:00
      selector:
        duration:

block_ended: *trigger_common
block_started: *trigger_common
