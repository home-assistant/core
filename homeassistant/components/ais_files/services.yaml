# Describes the format for available ais files services

refresh_files:
  description: Regresch filef in sensor from folder gallery location

remove_file:
  description: Remove file from gallery
  fields:
    path:
      description: Path to file.
      example: '/local/test.png'
