# Describes the format for available Remember The Milk services

create_task:
  fields:
    name:
      required: true
      example: "do this ^today #from_hass"
      selector:
        text:
    list_id:
      required: false
      example: "1234567"
      selector:
        text:
    id:
      example: myid
      selector:
        text:

complete_task:
  fields:
    id:
      required: true
      example: myid
      selector:
        text:
