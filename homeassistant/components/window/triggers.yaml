.trigger_common_fields: &trigger_common_fields
  behavior:
    required: true
    default: any
    selector:
      automation_behavior:
        mode: trigger
  for:
    required: true
    default: 00:00:00
    selector:
      duration:

closed:
  fields: *trigger_common_fields
  target:
    entity:
      - domain: binary_sensor
        device_class: window
      - domain: cover
        device_class: window

opened:
  fields: *trigger_common_fields
  target:
    entity:
      - domain: binary_sensor
        device_class: window
      - domain: cover
        device_class: window
