config_entry:
  versions:
    - version:
        major: 1
        minor: 1
      data:
        fields:
          selected_host_and_port:
            required: true
            selector:
              select:
                mode: dropdown
                options: []
      options:
        fields: {}
  subentries: {}
