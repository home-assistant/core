language: es
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - pon <temp> a <temperature>
          - pon <temp> <area> a <temperature>
          - pon <area> <temp> a <temperature>
  HassClimateGetTemperature:
    data:
      - sentences:
          - cuál es [la] <temp>
          - qué <temp> hace
          - cuál es [la] <temp> <area>
          - qué <temp> hace en <area>
responses:
  intents:
    HassClimateGetTemperature:
      success:
        - "{{ state.state }} grados"
