language: it
intents:
  HassClimateSetTemperature:
    data:
      - sentences:
          - imposta <temp> a <temperature>
          - imposta [<temp>] [<in>] <area> [a] <temperature>
          - imposta [<temp>] [di] <temperature> [<in>] <area>
          - imposta [<in>] <area> [<temp>] [di] <temperature>
          - Imposta <temp> <area> [a] <temperature>
  HassClimateGetTemperature:
    data:
      - sentences:
          - <what_is> <temp>
          - "[(<what_is> | che)] <temp> [c'è]"
          - <what_is> <temp> [(<in> | <of>)] <area>
          - quanto (caldo | freddo) [(è | c'è | fa)] [(<in>|<the>) <area>]
          - quanto [(è | c'è | fa)] (caldo | freddo) [(<in>|<the>) <area>]
          - "[(<what_is> | che)] <temp> [c'è] [<in>] <area>"
responses:
  intents:
    HassClimateGetTemperature:
      success:
        - "{{ state.state }} gradi"
