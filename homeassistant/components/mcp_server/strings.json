{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    },
    "create_entry": {
      "default": "The Model Context Protocol Server is active. To expose only some LLM APIs, select **Configure** on the integration."
    },
    "step": {
      "user": {
        "description": "The Model Context Protocol Server exposes all LLM APIs. See the [integration documentation]({more_info_url}) for setup instructions."
      }
    }
  },
  "options": {
    "error": {
      "llm_api_required": "Select at least one LLM API or expose all LLM APIs."
    },
    "step": {
      "init": {
        "data": {
          "all_llm_apis": "Expose all LLM APIs",
          "llm_hass_api": "[%key:common::config_flow::data::llm_hass_api%]",
          "require_admin": "Require an administrator account"
        },
        "data_description": {
          "all_llm_apis": "Expose every LLM API, including LLM APIs added later. When enabled, the selected LLM APIs are ignored.",
          "llm_hass_api": "The methods for controlling Home Assistant to expose with the Model Context Protocol.",
          "require_admin": "Only allow administrator accounts to use the Model Context Protocol endpoint."
        },
        "description": "See the [integration documentation]({more_info_url}) for setup instructions."
      }
    }
  }
}
