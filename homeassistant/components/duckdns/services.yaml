set_txt:
  fields:
    config_entry_id:
      required: true
      selector:
        config_entry:
          integration: duckdns
    txt:
      example: "This domain name is reserved for use in documentation"
      selector:
        text:
