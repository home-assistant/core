# Describes the format for available file services
read_file:
  fields:
    file_name:
      example: "my_file.json"
      selector:
        text:
    file_encoding:
      example: "Energize"
      selector:
        select:
          options:
            - "json"
            - "yaml"
