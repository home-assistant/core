{
  "config": {
    "step": {
      "user": {
        "title": "Fill in your filter details.",
        "data": { "radius": "Radius" }
      }
    },
    "abort": { "already_configured_location": "[%key:common::config_flow::abort::already_configured_location%]" }
  }
}
