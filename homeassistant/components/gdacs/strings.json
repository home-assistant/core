{
  "config": {
    "step": {
      "user": {
        "title": "Fill in your filter details.",
        "data": { "radius": "Radius" }
      }
    },
    "abort": { "already_configured": "Location is already configured." }
  }
}
