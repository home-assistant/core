.trigger_common: &trigger_common
  target:
    entity:
      domain: update
  fields:
    behavior:
      required: true
      default: any
      selector:
        automation_behavior:
          mode: trigger
    for:
      required: true
      default: 00:00:00
      selector:
        duration:

update_became_available: *trigger_common
