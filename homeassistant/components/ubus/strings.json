{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]",
      "reauth_successful": "[%key:common::config_flow::abort::reauth_successful%]"
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]"
    },
    "step": {
      "reauth_confirm": {
        "data": {
          "password": "[%key:common::config_flow::data::password%]",
          "username": "[%key:common::config_flow::data::username%]"
        },
        "description": "The credentials for OpenWrt (ubus) at {host} were rejected. Enter the current username and password to reconnect."
      },
      "user": {
        "data": {
          "dhcp_software": "DHCP software",
          "host": "[%key:common::config_flow::data::host%]",
          "password": "[%key:common::config_flow::data::password%]",
          "username": "[%key:common::config_flow::data::username%]"
        },
        "data_description": {
          "dhcp_software": "The DHCP server running on the router, used to resolve device hostnames."
        }
      }
    }
  },
  "issues": {
    "yaml_import_cannot_connect": {
      "description": "The YAML configuration for OpenWrt (ubus) at `{host}` could not be imported because the connection failed.\n\nPlease check that `{host}` is reachable, update your `configuration.yaml` if needed, and restart Home Assistant.",
      "title": "YAML import failed: cannot connect"
    },
    "yaml_import_invalid_auth": {
      "description": "The YAML configuration for OpenWrt (ubus) at `{host}` could not be imported because the credentials were rejected.\n\nPlease check the username and password in your `configuration.yaml`, then restart Home Assistant.",
      "title": "YAML import failed: invalid authentication"
    }
  },
  "selector": {
    "dhcp_software": {
      "options": {
        "dnsmasq": "dnsmasq",
        "none": "None",
        "odhcpd": "odhcpd"
      }
    }
  }
}
