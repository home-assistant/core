language: da
intents:
  HassTurnOff:
    data:
      - sentences:
          - sluk {name}(en | et)
  HassTurnOn:
    data:
      - sentences:
          - "t\xE6nd {name}(en | et)"
responses:
  errors:
    no_intent: "Undskyld, jeg kunne ikke forst\xE5 det"
    no_area: "Omr\xE5det med navnet {{ area }} findes ikke"
    no_domain: "{{ domain }} findes ikke i omr\xE5det {{ area }}"
    no_device_class: "{{ device_class }} findes ikke i omr\xE5det {{ area }}"
    no_entity: "{{ entity }} findes ikke"
    handle_error: Noget gik galt med at forarbejde hensigten
lists: {}
expansion_rules: {}
skip_words: []
