is_event_active:
  target:
    entity:
      - domain: calendar
  fields:
    behavior:
      required: true
      default: any
      selector:
        automation_behavior:
          translation_key: condition_behavior
          behavior:
            - all
            - any
    for:
      required: true
      default: 00:00:00
      selector:
        duration:
