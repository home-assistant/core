is_event_active:
  target:
    entity:
      - domain: calendar
  fields:
    behavior:
      required: true
      default: any
      selector:
        select:
          translation_key: condition_behavior
          options:
            - all
            - any
