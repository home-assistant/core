.trigger_common: &trigger_common
  target:
    entity:
      domain: calendar
  fields:
    offset:
      required: true
      default:
        type: none
      selector:
        offset:
          enable_day: true

event_started: *trigger_common
event_ended: *trigger_common
