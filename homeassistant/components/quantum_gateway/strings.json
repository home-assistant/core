{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]",
      "reauth_successful": "[%key:common::config_flow::abort::reauth_successful%]"
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]"
    },
    "step": {
      "reauth_confirm": {
        "description": "There was an error logging into the Quantum Gateway. Please check the password and configuration.",
        "title": "[%key:common::config_flow::title::reauth%]"
      },
      "user": {
        "data": {
          "host": "[%key:common::config_flow::data::host%]",
          "password": "[%key:common::config_flow::data::password%]",
          "ssl": "[%key:common::config_flow::data::ssl%]"
        }
      }
    }
  },
  "exceptions": {
    "invalid_auth": {
      "message": "[%common::config_flow::error::invalid_auth%]"
    }
  },
  "issues": {
    "yaml_import_cannot_connect": {
      "description": "The YAML configuration for Quantum Gateway at `{host}` could not be imported because the connection failed.\n\nPlease check that `{host}` is reachable, update your `configuration.yaml` if needed, and restart Home Assistant.",
      "title": "YAML configuration import failed: cannot connect"
    },
    "yaml_import_invalid_auth": {
      "description": "The YAML configuration for Quantum Gateway at `{host}` could not be imported because login failed.\n\nPlease check the password, update your `configuration.yaml` if needed, and restart Home Assistant.",
      "title": "YAML configuration import failed: cannot authenticate"
    }
  }
}
