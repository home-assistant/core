{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "description": "Choose a device to setup",
        "data": {
          "address": "Device"
        }
      },
      "bluetooth_confirm": {
        "description": "Do you want to setup {name}?"
      },
      "default_adapter": {
        "description": "Do you want to setup the Bluetooth adapter {name}?"
      },
      "discovered_adapter": {
        "description": "[%key:component::bluetooth::config::step::default_adapter::description%]"
      }
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_service%]",
      "no_adapters": "No Bluetooth adapters found"
    }
  }
}
