.trigger_common: &trigger_common
  target:
    entity:
      domain: timer
  fields:
    events:
      required: true
      selector:
        select:
          options:
            - start
            - finish
            - pause
            - cancel
            - restart
          translation_key: timer_events
          multiple: true

events: *trigger_common
