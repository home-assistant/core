get_last_data:
  fields:
    envoy:
      required: true
      selector:
        entity:
          integration: enphase_envoy
          domain: sensor
    endpoint:
      required: false
      example: "/api/v1/production/inverters,0,maxReportWatts"
      selector:
        text:
get_current_data:
  fields:
    envoy:
      required: true
      selector:
        entity:
          integration: enphase_envoy
          domain: sensor
    endpoint:
      required: true
      example: "/api/v1/production/inverters,0,maxReportWatts"
      selector:
        text:
post_data:
  fields:
    envoy:
      required: true
      selector:
        entity:
          integration: enphase_envoy
          domain: sensor
    endpoint:
      required: true
      selector:
        text:
    data:
      required: true
      example: "{'do_not_use': 'if you do not know what impact will be'}"
      selector:
        text:
    method:
      required: false
      default: "POST"
      example: "POST"
      selector:
        select:
          options:
            - "POST"
            - "PUT"
get_firmware:
  fields:
    envoy:
      required: true
      selector:
        entity:
          integration: enphase_envoy
          domain: sensor
