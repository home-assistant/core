{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "unknown": "[%key:common::config_flow::error::unknown%]"
    },
    "step": {
      "user": {
        "data": {
          "device_key": "Device key",
          "host": "[%key:common::config_flow::data::host%]",
          "port": "[%key:common::config_flow::data::port%]",
          "verify_ssl": "[%key:common::config_flow::data::verify_ssl%]"
        },
        "data_description": {
          "host": "The hostname or IP address of your OpenGarage device."
        }
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "obstruction": {
        "name": "Obstruction"
      },
      "vehicle": {
        "name": "Vehicle"
      }
    },
    "light": {
      "light": {
        "name": "Light"
      }
    },
    "lock": {
      "lock": {
        "name": "Remote control lock"
      }
    },
    "sensor": {
      "openings": {
        "name": "Openings"
      }
    }
  },
  "services": {
    "reset_wifi": {
      "description": "Resets the device to access-point mode for Wi-Fi configuration. The device goes offline until its Wi-Fi is reconfigured. Requires administrator access.",
      "fields": {
        "config_entry_id": {
          "description": "The OpenGarage device to reset.",
          "name": "Device"
        }
      },
      "name": "Reset Wi-Fi"
    }
  }
}
