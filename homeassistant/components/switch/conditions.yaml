.condition_common: &condition_common
  target:
    entity:
      - domain: switch
      - domain: input_boolean
  fields:
    behavior:
      required: true
      default: any
      selector:
        automation_behavior:
          mode: condition

is_off: *condition_common
is_on: *condition_common
