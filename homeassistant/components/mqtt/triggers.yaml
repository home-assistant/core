# Describes the format for MQTT triggers

mqtt:
  fields:
    payload:
      example: "on"
      required: false
      selector:
        text:
    topic:
      example: "living_room/switch/ac"
      required: true
      selector:
        text:
