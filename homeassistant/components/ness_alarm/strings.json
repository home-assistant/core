{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Ness Alarm",
        "description": "Set up your alarm panel. The integration will automatically detect your panel model and configure the appropriate number of zones.",
        "data": {
          "host": "Host",
          "port": "Port",
          "scan_interval": "Scan Interval (seconds)",
          "infer_arming_state": "Infer Arming State"
        },
        "data_description": {
          "host": "The hostname or IP address of the IP232 module on your network",
          "port": "The port on which the IP232 module listens for clients (default: 2401)",
          "scan_interval": "Time interval between updates in seconds (default: 60)",
          "infer_arming_state": "Infer the disarmed arming state only via system status events. This works around a bug with some panels (version less than 5.8) which emit empty status updates when they are armed"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the IP232 module",
      "invalid_auth": "Invalid authentication",
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "This Ness alarm panel is already configured",
      "cannot_connect": "Failed to connect to the IP232 module during import"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Ness Alarm Options",
        "description": "Adjust settings for your Ness alarm panel.",
        "data": {
          "scan_interval": "Scan Interval (seconds)",
          "infer_arming_state": "Infer Arming State",
          "support_home_arm": "Support Home Arm",
          "enabled_zones": "Number of Active Zones"
        },
        "data_description": {
          "scan_interval": "Time interval between updates in seconds",
          "infer_arming_state": "Infer the disarmed arming state only via system status events. This works around a bug with some panels (version less than 5.8) which emit empty status updates when they are armed",
          "support_home_arm": "Enable support for Home arming (ARM_HOME)",
          "enabled_zones": "Number of zones to enable. Zones beyond this number will be disabled but can be manually re-enabled. Use this to override auto-detection or if you have zone expanders."
        }
      }
    }
  },
  "services": {
    "aux": {
      "name": "Aux",
      "description": "Changes the state of an aux output.",
      "fields": {
        "output_id": {
          "name": "Output ID",
          "description": "The aux output you wish to change."
        },
        "state": {
          "name": "State",
          "description": "The on/off state of the output. If P14xE 8E is enabled then turning on will pulse the output for the time specified in P14(x+4)E."
        }
      }
    },
    "panic": {
      "name": "Panic",
      "description": "Triggers a panic alarm.",
      "fields": {
        "code": {
          "name": "Code",
          "description": "The user code to use to trigger the panic alarm."
        }
      }
    }
  }
}
