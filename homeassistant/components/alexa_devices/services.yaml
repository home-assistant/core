send_text_command:
  target:
    entity:
      integration: amazon_devices
      domain: notify
    device:
      integration: amazon_devices
  fields:
    text_command:
      required: true
      example: "Play B.B.C. on TuneIn"
      selector:
        text:

send_sound:
  target:
    entity:
      integration: amazon_devices
      domain: notify
    device:
      integration: amazon_devices
  fields:
    sound:
      required: true
      example: "amzn_sfx_doorbell_chime_01"
      selector:
        text:
