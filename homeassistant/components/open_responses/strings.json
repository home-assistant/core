{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_service%]",
      "reauth_successful": "[%key:common::config_flow::abort::reauth_successful%]"
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "invalid_model": "The endpoint rejected the configured model.",
      "rate_limited": "The endpoint is rate limited. Please try again later.",
      "unknown": "[%key:common::config_flow::error::unknown%]"
    },
    "step": {
      "reauth_confirm": {
        "data": {
          "api_key": "[%key:common::config_flow::data::api_key%]",
          "base_url": "[%key:component::open_responses::config::step::user::data::base_url%]",
          "model": "Model"
        },
        "data_description": {
          "api_key": "[%key:component::open_responses::config::step::user::data_description::api_key%]",
          "base_url": "[%key:component::open_responses::config::step::user::data_description::base_url%]",
          "model": "The model ID to use for setup validation and default entities."
        },
        "description": "Reauthentication required. Please enter your updated connection details."
      },
      "user": {
        "data": {
          "api_key": "API key",
          "base_url": "Base URL",
          "model": "Model"
        },
        "data_description": {
          "api_key": "The API key for your Open Responses-compatible endpoint.",
          "base_url": "The full API base URL for an endpoint that implements Open Responses.",
          "model": "The model ID to validate and use for the default conversation entity."
        },
        "description": "Set up Open Responses by providing an API key and an Open Responses-compatible base URL."
      }
    }
  },
  "config_subentries": {
    "conversation": {
      "abort": {
        "entry_not_loaded": "Cannot add things while the configuration is disabled.",
        "reconfigure_successful": "[%key:common::config_flow::abort::reconfigure_successful%]"
      },
      "entry_type": "Conversation agent",
      "initiate_flow": {
        "reconfigure": "Reconfigure conversation agent",
        "user": "Add conversation agent"
      },
      "step": {
        "init": {
          "data": {
            "llm_hass_api": "[%key:common::config_flow::data::llm_hass_api%]",
            "max_output_tokens": "Maximum output tokens",
            "model": "[%key:common::generic::model%]",
            "name": "[%key:common::config_flow::data::name%]",
            "prompt": "[%key:common::config_flow::data::prompt%]",
            "store_responses": "Store responses"
          },
          "data_description": {
            "prompt": "Instruct how the LLM should respond. This can be a template.",
            "store_responses": "If enabled, the compatible endpoint may store request and response data."
          }
        }
      }
    }
  },
  "exceptions": {
    "invalid_config_entry": {
      "message": "Invalid config entry provided. Got {config_entry}"
    }
  }
}
