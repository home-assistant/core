notify:
  # Show a rich notification
  fields:
    device_id:
      required: false
      selector:
        device:
          integration: quickbars
    title:
      selector:
        text:
      required: false
    message:
      selector:
        text:
          multiline: true
      required: true
    actions:
      selector:
        object:
      required: false
      example: [{ "id": "open", "label": "Open" }]
    color:
      selector:
        color_rgb:
      required: false
      example: { "r": 255, "g": 165, "b": 0 }
    transparency:
      selector:
        number:
          min: 0
          max: 1
          step: 0.05
      required: false
      default: 0.0
    mdi_icon:
      selector:
        icon:
      required: false
      example: mdi:bell
    image:
      selector:
        object:
      required: false
      example: { "url": "https://example.com/pic.jpg" }
    image_media:
      selector:
        media:
          accept:
            - image/*
      required: false
    sound:
      selector:
        object:
      required: false
      example: { "url": "https://example.com/file.mp3" }
    sound_media:
      selector:
        media:
          accept:
            - audio/*
      required: false
    sound_volume_percent:
      selector:
        number:
          min: 0
          max: 200
      required: false
      default: 100
    length:
      default: 10
      selector:
        number:
          min: 0
          max: 300
          unit_of_measurement: "seconds"
      required: false
    interrupt:
      default: false
      selector:
        boolean:
      required: false
    position:
      selector:
        select:
          options: [top_left, top_right, bottom_left, bottom_right]
      required: false
