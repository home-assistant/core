sign_in:
  fields:
    username:
      required: true
      example: "example@example.com"
      selector:
        text:
    password:
      required: true
      example: "password"
      selector:
        text:

sign_out:
