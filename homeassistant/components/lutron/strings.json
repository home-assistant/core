{
  "config": {
    "flow_title": "Lutron Setup",
    "step": {
      "user": {
        "title": "Lutron Setup",
        "description": "Please enter the Main Repeater login information",
        "data": {
          "host": "Main Repeater IP Address",
          "username": "[%key:common::config_flow::data::username%]",
          "password": "[%key:common::config_flow::data::password%]"
        }
      }
    },
    "abort": {
      "already_configured": "Device is already configured",
      "config_errors": "Errors Found in Configuration",
      "single_instance": "Only a single instance may be configured"
    }
  }
}
