# To be removed

# min_max
sensor:
  - platform: min_max
    entity_ids:
      - sensor.kitchen_temperature
      - sensor.living_room_temperature
      - sensor.office_temperature
    type: max
    name: Test
    round_digits: 2 # Not exist in groups
    unique_id: something_unique
  # group
  - platform: group
    entities: # from entity_ids
      - sensor.kitchen_temperature
      - sensor.living_room_temperature
      - sensor.office_temperature
    type: max
    name: Test
    unique_id: something_unique
    ignore_non_numeric: false # Not exist in min_max
    unit_of_measurement: °C # Not exist in min_max
    device_class: temperature # Not exist in min_max
    state_class: measurement # Not exist in min_max
