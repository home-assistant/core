# Velux Integration services

set_velocity:
  target:
    device:
      integration: velux
  fields:
    velocity:
      required: true
      selector:
        select:
          options:
            - "default"
            - "silent"
            - "fast"
