set_keyboard_text:
  target:
    entity:
      integration: apple_tv
      domain: remote
  fields:
    text:
      required: true
      example: "The Wizard of Oz"
      selector:
        text:

append_keyboard_text:
  target:
    entity:
      integration: apple_tv
      domain: remote
  fields:
    text:
      required: true
      example: "a Mockingbird"
      selector:
        text:

clear_keyboard_text:
  target:
    entity:
      integration: apple_tv
      domain: remote
