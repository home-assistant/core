"""``python -m hass_client.sandbox_v2`` — sandbox runtime entry point.

This subpackage exists only so the runtime can be launched via ``-m``.
The actual implementation lives in :mod:`hass_client.sandbox`.
"""
