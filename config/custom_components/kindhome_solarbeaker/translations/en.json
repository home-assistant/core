{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "description": "custom user step description",
        "data": {
          "address": "custom address description"
        }
      },
      "bluetooth_confirm": {
        "title": "Confirm integrating this device",
        "description": "custom bluetooth confirm description"
      }
    },
    "abort": {
      "no_devices_found": "No kindhome solarbeaker devices found!",
      "already_in_progress": "custom already in progress description",
      "already_configured": "custom already configured description"
    }
  }
}
